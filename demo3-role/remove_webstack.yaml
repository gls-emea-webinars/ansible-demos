---
- name: Remove the Webservers with a Role
  hosts: webservers

  vars:
    modus: remove

  roles:
    - webserver


- name: Remove the loadbalancer with a Role
  hosts: loadbalancers

  vars:
    modus: remove

  roles:
    - loadbalancer